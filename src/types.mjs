/**
 * This file includes the types used in this project.
 * Switching to TypeScript would be much better but here we are...
 */

/**
 * @typedef {object} GitHubContributor
 * @property {string} login
 * @property {number} contributions
 * @property {string} avatar_url
 */

/**
 * @typedef {object} SrcContributors
 * @property {string} login
 * @property {string} avatar_url
 * @property {string} name
 * @property {string} profile
 * @property {string[]} contributions
 */

// TODO: Add the GitHub API types
// (there might be an existing npm package that does that, but be we only need 2/3 types, so we might as well just write them here)
